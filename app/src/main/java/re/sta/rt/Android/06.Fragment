


Fragment
- Activity -> 앱에 보이는 한 화면의 단위

언제 필요할까?
    - 액티비티가 길어지게 되면 (파트1,2,3,4,5,6 등) --> 관리 포인트가 많아진다 -> 관리가 어려워진다
    - 안드로이드 디바이스가 너무 다양하다.
    - 안드로이드에는 핸드폰만 있는게 아니다! -> 테블릿도 있다. (모바일과 태블릿의 화면 구도?구성이 완전히 다르다)
        화면이 큰 테블릿은 dp 단위로 다룰 수가 없다.
